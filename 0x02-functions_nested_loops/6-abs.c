#include "main.h"

/**
  * _abs - A functin to compute the absolute value of an integer
  * Return: Absolute value of number or zero
  */

int _abs(int i)
{
	if (i < 0)
	{
		int abs;
		abs = i * -1;
		return (abs);
	}
	return (i)
}
